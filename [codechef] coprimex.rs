fn main() {
    let mut line = String::new();
    let t: u32;
    std::io::stdin().read_line(&mut line).unwrap();
    t = line.trim().parse().unwrap();
    for _ in 0..t {
        println!("{}", 1000003);
    }
}
